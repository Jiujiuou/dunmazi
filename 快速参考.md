# 🚀 快速参考 - 开发者备忘

## 📦 常用命令

```bash
# 安装依赖
npm install

# 启动开发服务器（端口 3000）
npm run dev

# 构建生产版本
npm build

# 预览生产构建
npm run preview

# 代码检查
npm run lint
```

## 📁 文件快速定位

| 需要修改/查看 | 文件位置 |
|--------------|---------|
| 游戏配置常量 | `src/constants/gameConfig.js` |
| 创建/加入房间逻辑 | `src/stores/gameStore.js` |
| 大厅页面 UI | `src/components/Lobby.jsx` |
| 大厅样式 | `src/components/Lobby.css` |
| 游戏房间 UI | `src/components/GameRoom.jsx` |
| 游戏房间样式 | `src/components/GameRoom.css` |
| Supabase 配置 | `src/config/supabase.js` |
| 环境变量 | `.env` |
| 全局样式 | `src/index.css` |

## 🎨 设计系统

### 颜色变量

```css
/* 主色调 */
--primary: #4db6ac;
--primary-dark: #26a69a;
--primary-light: #80cbc4;

/* 背景色 */
--bg-gradient-start: #e8f5e9;
--bg-gradient-mid: #e0f2f1;
--bg-gradient-end: #e1f5fe;

/* 文字色 */
--text-primary: #37474f;
--text-secondary: #78909c;
--text-light: #b0bec5;

/* 状态色 */
--success: #66bb6a;
--error: #e57373;
--warning: #ffd54f;
```

### 常用尺寸

```css
/* 圆角 */
--radius-sm: 1vh;
--radius-md: 1.5vh;
--radius-lg: 2vh;
--radius-xl: 3vh;

/* 阴影 */
--shadow-sm: 0 0.5vh 2vh rgba(0,0,0,0.05);
--shadow-md: 0 1vh 3vh rgba(0,0,0,0.08);
--shadow-lg: 0 2vh 6vh rgba(0,0,0,0.08);

/* 间距 */
--spacing-xs: 1vh;
--spacing-sm: 2vh;
--spacing-md: 3vh;
--spacing-lg: 5vh;
```

### 字体

```css
/* 标题字体 */
font-family: 'DM Serif Display', serif;

/* 正文字体 */
font-family: 'Outfit', sans-serif;

/* 字号 */
--text-xs: 1.4vh;
--text-sm: 1.6vh;
--text-base: 1.8vh;
--text-lg: 2vh;
--text-xl: 2.2vh;
--text-2xl: 3vh;
--text-3xl: 4vh;
--text-4xl: 5vh;
```

## 🔧 Zustand Store API

### gameStore 方法

```javascript
// 创建房间
const createGame = async (nickname) => { ... }

// 加入房间
const joinGame = async (roomCode, nickname) => { ... }

// 离开房间
const leaveGame = async () => { ... }

// 订阅实时更新
const subscribeToGame = (gameId) => { ... }

// 清除错误
const clearError = () => { ... }
```

### Store 状态

```javascript
{
  currentPlayer: null,     // 当前玩家信息
  game: null,              // 游戏信息
  players: [],             // 玩家列表
  loading: false,          // 加载状态
  error: null,             // 错误信息
}
```

## 🗃️ Supabase 数据库结构

### games 表

```sql
- id (uuid)
- room_code (text, unique)
- status (text) -- 'waiting', 'playing', 'finished'
- host_id (uuid)
- current_turn (integer)
- game_state (jsonb)
- deck (jsonb)
- played_cards (jsonb)
- created_at (timestamp)
- updated_at (timestamp)
```

### players 表

```sql
- id (uuid)
- game_id (uuid, FK)
- nickname (text)
- position (integer)
- hand (jsonb)
- player_state (jsonb)
- is_online (boolean)
- joined_at (timestamp)
```

## 🎯 常用代码片段

### 在组件中使用 Store

```jsx
import { useGameStore } from '../stores/gameStore'

function MyComponent() {
  const { game, players, createGame } = useGameStore()
  
  // 使用状态和方法
}
```

### 添加新的游戏状态

```javascript
// 在 stores/gameStore.js 中添加
newMethod: async () => {
  set({ loading: true })
  try {
    // 逻辑代码
    const { data, error } = await supabase
      .from('games')
      .update({ ... })
      .eq('id', gameId)
    
    if (error) throw error
    set({ game: data })
  } catch (error) {
    set({ error: error.message })
  } finally {
    set({ loading: false })
  }
}
```

### 创建新常量

```javascript
// 在 constants/ 文件夹创建新文件
export const MY_CONSTANT = {
  KEY: 'value',
}
```

### 添加工具函数

```javascript
// 在 utils/ 文件夹创建新文件
export const myUtilFunction = (param) => {
  // 逻辑代码
  return result
}
```

## 🐛 调试技巧

### 查看 Store 状态

```jsx
const store = useGameStore()
console.log('Store state:', store)
```

### 查看 Supabase 响应

```javascript
const { data, error } = await supabase.from('games').select('*')
console.log('Data:', data)
console.log('Error:', error)
```

### 查看实时订阅

```javascript
subscribeToGame: (gameId) => {
  supabase
    .channel(`game:${gameId}`)
    .on('postgres_changes', ..., (payload) => {
      console.log('Realtime update:', payload)
    })
}
```

## 📱 响应式断点

```css
/* 移动端 */
@media (max-width: 768px) {
  /* 样式调整 */
}
```

## ⌨️ VSCode 推荐插件

- ES7+ React/Redux/React-Native snippets
- ESLint
- Prettier
- Auto Rename Tag
- Color Highlight

## 🔗 有用的链接

- [React 文档](https://react.dev)
- [Vite 文档](https://vitejs.dev)
- [Zustand 文档](https://docs.pmnd.rs/zustand)
- [Supabase 文档](https://supabase.com/docs)
- [CSS Tricks](https://css-tricks.com)

## 💡 开发提示

1. **保持常量同步**: 修改配置时记得更新 `constants/gameConfig.js`
2. **测试实时同步**: 用多个浏览器窗口测试
3. **查看控制台**: 所有错误都会在控制台显示
4. **检查 Supabase 日志**: Dashboard → Logs 查看数据库操作
5. **使用 vh/vw**: 保持响应式，不要用固定像素

## 🎨 添加新页面/组件

1. 在 `src/components/` 创建组件文件
2. 创建对应的 CSS 文件
3. 在 `App.jsx` 中引入和使用
4. 更新状态管理（如需要）

## 🚀 部署检查清单

- [ ] 环境变量已配置
- [ ] 生产构建无错误 (`npm run build`)
- [ ] 测试所有功能
- [ ] 检查移动端适配
- [ ] 优化资源大小
- [ ] 配置 Vercel/Netlify

---

**快速开始**: `npm install && npm run dev` 🎮
