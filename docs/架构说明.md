# 🎴 蹲麻子 - 架构说明和开发指南

## 📊 项目架构总览

```
前端 (React + Vite)
    ↓
Zustand Store (gameStore，状态管理)
    ↓
Supabase Client (数据库客户端)
    ↓
Supabase Realtime (实时同步)
    ↓
PostgreSQL (数据库)
```

## 🎯 核心设计理念

### 1. **通用数据结构 + 灵活游戏规则**

数据库表结构是**通用的**，不绑定任何具体游戏规则：

```javascript
// ✅ 通用：适用于任何扑克游戏
{
  game_state: {...},      // JSON：存储任何游戏状态
  player_state: {...},    // JSON：存储任何玩家状态
  hand: [...],            // JSON：存储手牌
}

// ❌ 不好：绑定到具体游戏
{
  is_landlord: true,      // 只适用于斗地主
  chips: 1000,            // 只适用于德州扑克
}
```

**优势**：

- ✅ 可以随时更换游戏规则
- ✅ 可以同时支持多种游戏
- ✅ 数据库结构稳定，不需要频繁修改

### 2. **前端处理游戏逻辑**

游戏规则逻辑在**前端**实现：`src/stores/gameStore.js`（流程与状态）、`src/utils/handEvaluation.js`（牌型与扣牌条件）、`src/utils/compareHands.js`（比牌与结算）。后端只负责存储数据。

**为什么这样设计？**

- ✅ 你不需要学习后端框架
- ✅ 开发和调试更简单
- ✅ 适合小型多人游戏

**是否安全？**

- 基础版本：使用 Supabase RLS（Row Level Security）限制数据访问
- 进阶版本：可以添加服务端验证（Supabase Edge Functions）

---

## 📁 文件结构和职责

### 核心文件

```
src/
├── config/
│   └── supabase.js              [配置] Supabase 连接
│
├── stores/
│   └── gameStore.js             [状态] 游戏状态与流程
│                                 - 创建/加入游戏、实时同步
│                                 - 摸牌/出牌/换牌/清场/扣牌/响应/结算
│
├── utils/
│   ├── cardUtils.js             [工具] 扑克牌通用函数（创建牌堆、洗牌、发牌、排序）
│   ├── handEvaluation.js        [规则] 牌型评估与扣牌条件
│   │                             - 同花色判定、手牌分、麻子判定
│   └── compareHands.js          [规则] 比牌与结算
│                                 - 三级比牌、竞争池、奖池分配
│
├── components/
│   ├── Card.jsx                 [UI] 单张扑克牌
│   ├── Lobby.jsx                [UI] 大厅（创建/加入房间）
│   ├── GameRoom.jsx             [UI] 游戏主界面
│   ├── ActionLog.jsx            [UI] 出牌/换牌/扣牌等操作记录
│   ├── SettlementModal.jsx      [UI] 结算弹窗
│   ├── PlayerPosition.jsx       [UI] 对手位置与手牌数
│   └── …                        [UI] 其他组件（PlayArea、DeckPile、ShowdownBanner 等）
│
└── App.jsx                      [入口] 按是否有 game/currentPlayer 切换 Lobby / GameRoom
```

---

## 🎮 本项目的游戏规则实现方式

蹲麻子规则已在以下位置实现：

- **配置**：`src/constants/gameConfig.js`（人数、目标分、回合数等）
- **牌型与扣牌条件**：`src/utils/handEvaluation.js`（同花、手牌分、`canKnock`）
- **比牌与结算**：`src/utils/compareHands.js`（`compareHands`、`determineWinner`、`calculateScores`）
- **流程与状态**：`src/stores/gameStore.js`（摸牌、出牌、换牌、清场、扣牌、响应、结算）

若要扩展或修改规则，可优先改 `gameConfig.js`、`handEvaluation.js`、`compareHands.js`，再在 `gameStore.js` 中接入。

---

## 🔄 数据流程图

### 创建游戏流程

```
1. 用户点击"创建房间"
   ↓
2. gameStore.createGame()
   ↓
3. 生成房间号 → 插入 games 表
   ↓
4. 创建玩家 → 插入 players 表
   ↓
5. 订阅实时更新 → subscribeToGame()
   ↓
6. 跳转到游戏房间页面
```

### 出牌流程

```
1. 用户选择卡牌 → 点击"出牌"
   ↓
2. gameStore.playCard(card)
   ↓
3. 调用 gameLogic.canPlayCard() 验证
   ↓
4. 验证通过 → 更新 Supabase
   - 更新 players.hand（移除出的牌）
   - 更新 games.game_state（记录动作）
   - 插入 game_actions（记录历史）
   ↓
5. Supabase Realtime 推送变化
   ↓
6. 所有玩家收到更新 → UI 自动刷新
```

### 实时同步原理

```
玩家A                     Supabase                    玩家B
  |                          |                          |
  |--- 出牌 (更新数据库) ---->|                          |
  |                          |                          |
  |                          |------- Realtime 推送 ---->|
  |                          |                          |
  |<------ 确认更新 ---------|                          |
  |                          |                          |
  |                          |<------ 界面更新 ---------|
```

---

## 🛠️ 修改和扩展指南

### 场景 1：修改玩家数量

```javascript
// src/utils/gameLogic.js
export const GAME_CONFIG = {
  MIN_PLAYERS: 2, // 改为2人
  MAX_PLAYERS: 6, // 改为6人
};
```

### 场景 2：添加大小王

```javascript
export const GAME_CONFIG = {
  USE_JOKERS: true, // 改为 true
};
```

### 场景 3：修改每人手牌数量

```javascript
export const GAME_CONFIG = {
  CARDS_PER_PLAYER: 7, // 改为每人7张
};
```

### 场景 4：添加聊天功能

1. 创建 `messages` 表：

```sql
create table messages (
  id uuid default gen_random_uuid() primary key,
  game_id uuid references games(id),
  player_id uuid references players(id),
  content text not null,
  created_at timestamp with time zone default now()
);
```

2. 在 `gameStore.js` 添加发送消息方法
3. 在 `GameRoom` 组件添加聊天 UI

### 场景 5：添加音效

```javascript
// src/utils/sounds.js
export const playCardSound = () => {
  const audio = new Audio("/sounds/card.mp3");
  audio.play();
};

// 在出牌时调用
handlePlayCard = () => {
  playCardSound();
  // ... 其他逻辑
};
```

---

## 🎨 UI 自定义

### 修改扑克牌样式

编辑 `src/components/Card.css`（及 `Card.jsx` 中的尺寸 class，如 `card-small`、`card-micro`）：卡牌尺寸与花色颜色由 CSS 变量与 `data-suit` 控制。

### 修改主题颜色

主题色与语义色在 `src/index.css` 的 `:root` 中通过 CSS 变量定义（如 `--cta`、`--primary`、`--success` 等）。组件统一使用 `var(--…)`，详见 [docs/UI-Color-Analysis.md](./UI-Color-Analysis.md)。

---

## 🚀 部署到 Vercel

### 步骤

1. **推送代码到 GitHub**

```bash
git add .
git commit -m "Add poker game"
git push
```

2. **在 Vercel 导入项目**

- 访问 https://vercel.com
- 点击 "Import Project"
- 选择你的 GitHub 仓库

3. **配置环境变量**
   在 Vercel 项目设置中添加：

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

4. **部署**
   点击 "Deploy"，等待完成

5. **分享链接**
   部署完成后，你会得到一个网址（如 `https://your-app.vercel.app`）
   朋友访问 `https://your-app.vercel.app/poker` 即可游戏

---

## 📊 性能优化建议

### 1. 减少数据库查询

```javascript
// ❌ 不好：频繁查询
setInterval(() => {
  fetchPlayers();
}, 1000);

// ✅ 好：使用 Realtime 订阅
subscribeToGame(gameId);
```

### 2. 优化 Realtime 订阅

```javascript
// 只订阅需要的字段
.select('id, nickname, hand')  // 不是 select('*')
```

### 3. 添加加载状态

```javascript
const [loading, setLoading] = useState(false);

// 显示加载指示器
{
  loading && <div>处理中...</div>;
}
```

---

## 🐛 调试技巧

### 1. 查看 Supabase 数据

Supabase Dashboard → Table Editor → 查看实际数据

### 2. 查看 Realtime 消息

```javascript
.on('postgres_changes', ..., (payload) => {
  console.log('收到更新:', payload)  // 添加日志
})
```

### 3. 查看游戏状态

在组件中添加：

```javascript
console.log("当前游戏状态:", game);
console.log("当前玩家列表:", players);
```

---

## 📚 推荐学习资源

- **Supabase 文档**: https://supabase.com/docs
- **Zustand 文档**: https://docs.pmnd.rs/zustand
- **React Router 文档**: https://reactrouter.com

---

## ✨ 项目特点总结

### ✅ 你不需要学习的内容

- ❌ 后端框架（Express、NestJS 等）
- ❌ WebSocket 服务器搭建
- ❌ 数据库部署和运维
- ❌ 服务器配置

### ✅ 你只需要关注

- ✅ React 组件开发（你已经会了）
- ✅ 游戏规则逻辑（JavaScript）
- ✅ Supabase 基础操作（很简单）

### 🎯 核心优势

- 快速开发，专注游戏逻辑
- 实时同步开箱即用
- 免费部署（Vercel + Supabase）
- 容易扩展和维护

---

## 🎉 开始开发吧！

现在你已经了解了整个项目的架构，可以开始：

1. 按 [SUPABASE_SETUP.md](./SUPABASE_SETUP.md) 建表并配置环境变量
2. 运行 `npm run dev` 跑通创建房间、加入房间、对局与结算
3. 规则与 UI 按需在 `gameConfig`、`handEvaluation`、`compareHands`、`gameStore` 及各组件中调整
4. 部署（如 Vercel）时配置 `VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`

有任何问题，随时查看文档或在控制台查看错误信息。祝开发顺利！🚀
